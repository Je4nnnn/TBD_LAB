server:
  port: 8081
  servlet:
    context-path: /api   # <--- TODAS las rutas quedarán bajo /api

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dronesdb
    username: postgres
    password: 123
    hikari:
      maximum-pool-size: 10
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

# CORS: orígenes del frontend (ajusta si usas otro puerto)
app:
  cors:
    allowed-origins:
      - "http://localhost:5173"
      - "http://localhost:5174"
      - "http://localhost:8080"

# JWT demo (cámbialo en producción)
app.jwt:
  secret: "super-secreto-demostracion-pon-algo-mas-largo-porfa-1234567890"
  ttl-minutes: 120
